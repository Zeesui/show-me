
<h2>hello,world</h2>
<div class="skill-index">
	<div class="container" style="margin-top: -30px;>

			<% @skills.each do |skill| %>
      <%= render 'index_bar', skill: :skill %>
			<% end %>
		</div>
	</div>
