
<div class="skill-index">
	<div class="container" >

			<% @skills.each do |skill| %>
      <%= render :partial => "index_bar", :collection => @skills, :as => :skill %>
			<% end %>
		</div>
	</div>
